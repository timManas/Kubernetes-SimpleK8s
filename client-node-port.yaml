apiVersion: v1
kind: Service
metadata: 
    name: client-node-port
spec:
    type: NodePort
    ports:
        - ports: 3500
          targetPort: 3000
          nodePort: 31515
    selector: 
        component: wen      